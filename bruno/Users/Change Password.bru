meta {
  name: Change Password
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/users/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "oldPassword": "senha123",
    "newPassword": "novaSenha456"
  }
}
