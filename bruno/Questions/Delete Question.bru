meta {
  name: Delete Question
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/questions/texts/{{textId}}/questions/{{questionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  textId: your-text-id-here
  questionId: your-question-id-here
}
